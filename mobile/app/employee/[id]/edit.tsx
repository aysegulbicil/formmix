import { useQuery } from '@tanstack/react-query';import { useLocalSearchParams } from 'expo-router';import { api } from '../../../src/api';import { ErrorState, Loading, Screen } from '../../../src/components';import { EmployeeForm, type EmployeeEdit } from '../../../src/employeeForm';
export default function EditEmployee(){const{id}=useLocalSearchParams<{id:string}>();const q=useQuery({queryKey:['employee',id],queryFn:()=>api<{data:EmployeeEdit}>(`/employees/manage/${id}`)});if(q.isLoading)return<Loading/>;if(q.isError||!q.data)return<Screen><ErrorState retry={()=>void q.refetch()}/></Screen>;return <EmployeeForm employee={q.data.data}/>}
